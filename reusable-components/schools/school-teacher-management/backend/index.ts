/**
 * Public API for the school-teacher-management component.
 *
 * Consumers — including templates and other reusable components — must
 * import only from this file. Importing from internal modules
 * (types.ts, logic.ts, validation.ts) directly is forbidden because
 * those modules are not part of the stable contract.
 */

export * from "./types";
export * from "./validation";
export * from "./logic";
export * from "../config/defaults";
