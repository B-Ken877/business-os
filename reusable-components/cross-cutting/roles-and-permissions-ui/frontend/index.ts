/**
 * Frontend entry for roles-and-permissions-ui.
 *
 * Per ai-instructions/architecture-rules.md, the frontend must communicate
 * with the backend exclusively through the documented API surface in
 * backend/index.ts — never by reaching into another component's data.
 *
 * No production UI is implemented at this stage. When the platform's UI
 * layer is built, this folder will hold the React components, hooks, and
 * styles for this capability.
 */

export {};
